{
  "functions": {
    "api/inference.ts": {
      "maxDuration": 60,
      "includeFiles": "bin/**,models/**"
    }
  },
  "installCommand": "git lfs install && git lfs pull && npm install",
  "buildCommand": "npm run build",
  "outputDirectory": "."
}
