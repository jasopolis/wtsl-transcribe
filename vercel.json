{
  "functions": {
    "api/inference.ts": {
      "maxDuration": 60
    }
  },
  "installCommand": "git lfs install && git lfs pull && npm install",
  "buildCommand": "npm run build",
  "outputDirectory": "."
}
