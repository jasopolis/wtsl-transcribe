{
  "functions": {
    "api/inference.ts": {
      "memory": 1024,
      "maxDuration": 60
    }
  },
  "installCommand": "git lfs install && git lfs pull && git submodule update --init --recursive && npm install",
  "buildCommand": "npm run build"
}
