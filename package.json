{
  "name": "wtsl-transcribe",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "bash scripts/build-server.sh --for-deploy",
    "vercel-build": "npm run build"
  },
  "engines": {
    "node": "^24.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
