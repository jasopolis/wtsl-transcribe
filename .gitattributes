# Converted/quantized Whisper ggml models (Git LFS)
models/*.bin filter=lfs diff=lfs merge=lfs -text

# Prebuilt whisper-server binary (Git LFS) – Linux x64 for Vercel; build with scripts/build-server-linux.sh
bin/whisper-server filter=lfs diff=lfs merge=lfs -text
